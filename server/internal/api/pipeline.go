package api

import "github.com/gin-gonic/gin"

func (api *API) webhookPipeline(ctx *gin.Context) {
	// TODO: waiting to see what's required for github

	// TODO: create new pipeline
}

func (api *API) setPipelineStatus(ctx *gin.Context) {
}
